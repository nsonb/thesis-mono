import styled from "@emotion/styled";
import { default_theme } from "./Colors";

export const AppContainer = styled.div`
  background-color: ${default_theme.light_grey};
  margin: 0;
  padding: 0;
  min-height: 100vh;
  min-width: 100vw;
  position: relative;
  display: inline-block;
`